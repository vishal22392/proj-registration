apiVersion: apps/v1
kind: Deployment
metadata:
  name: register-app
  labels:
    app: register-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: register-app
  template:
    metadata:
      labels:
        app: register-app
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: register-app
        image: ashfaque9x/swiggy-clone:latest
        imagePullPolicy: "Always"
        ports:
        - containerPort: 8080